1.
Q: Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf?
A: a0, a1, a2. a2 is holding 13 in main's call.

2.
Q: Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.)
A: There is no call to function f and g, they are inlined.

3.
Q: At what address is the function `printf` located?
A: 642

4.
Q: What value is in the register `ra` just after the `jalr` to `printf` in `main`?
A: 0x38

5.
output: HE110 World
set i: 726c6400
don't have to change 57616, because it's not releted to what-endian.

6.
it's going to print whatever left in the a2 register before.
Where the arguments stores:  
![](https://cdn.jsdelivr.net/gh/PsyLinkist/LearningBlogPics@main/Materials/LearningBlogPics202401041715559.png)